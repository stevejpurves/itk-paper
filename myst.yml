# See docs at: https://mystmd.org/guide/frontmatter
version: 1
project:
  id: itk-paper-dev-directive
  title: simple itk viewer test
  # description:
  # keywords: []
  # authors: []
  # github:
  # To autogenerate a Table of Contents, run "myst init --write-toc"
  plugins:
    - itk.plugin.mjs
site:
  template: book-theme
  # options:
  #   favicon: favicon.ico
  #   logo: site_logo.png
